<nav>
    <div class="container">
        <a href="/" id="logo">Library Portal</a>
    </div>
</nav>

<div class="container main-contain">
    <div class="catalog-div">
        <div class="div-styling" id="catalog-search">
            <div id="catalog-form">
                <form>
                    <input type="search" placeholder="Find your next adventure..." id="catalog-search-bar">
                    <br>
                    <button class="standard-button" id="catalogExplorer"> EXPLORE</button>
                </form>
            </div>
            <div id="catalog-populate">
                {{#each books}}
                <p><strong>Title:</strong> {{this.title}}</p>
                <p><strong>Author: </strong> {{this.author}}</p>
                <p><strong>Description: </strong> {{this.description}}</p>
                {{/each}}
            </div>
        </div>
    </div>
</div>